本地方法栈：
1.Java虚拟机栈用于管理Jva方法的调用，而本地方法栈用于管理本地方法的调用。
2.本地方法栈也是线程私有的。
3.本地方法栈允许被实现成固定或者可以动态扩展的内存大小（同样也会存在SOF、OOM问题）。
4.本地方法是使用C语言实现的。
5.它的具体做法是Native Method Stack中登记native方法，在执行引擎执行时加载本地方法库。
6.当某一个线程调用一个本地方法时，它就进入了一个全新的并且不再受虚拟机限制的世界。它和虚拟机拥有同样的权限。
本地方法可以通过本地方法接口来访问虚拟机内部的运行时数据区。
它甚至可以直接使用处理器的寄存器。
直接从本地内存的堆中分配任意数量的内存（直接内存）。
7.并不是所有的JVM都支持本地方法，因为JVM规范中并没有明确要求本地方法栈的使用语言、具体实现方式、数据结构等。
8.HotSpot JVM中，直接将本地方法栈和虚拟机栈合二为一。